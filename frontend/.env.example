# Scripture API Bible
# Get your key from: https://scripture.api.bible/livedocs
VITE_SCRIPTURE_API_KEY=your_api_key_here

# Optional API base URL for production deployments (e.g. Render API service URL).
# Leave empty for local dev where Vite proxy handles /api.
VITE_API_BASE_URL=
